apiVersion: v1
kind: Service
metadata:
  name: netbox-mcp
  namespace: default
  labels:
    app: netbox-mcp
spec:
  selector:
    app: netbox-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
