apiVersion: v1
kind: Service
metadata:
  name: netbox-mcp-metrics
  labels:
    app: netbox-mcp
spec:
  selector:
    app: netbox-mcp
  ports:
    - name: metrics
      port: 8080
      targetPort: http
      protocol: TCP
