apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "netbox-mcp.fullname" . }}
  labels:
    {{- include "netbox-mcp.labels" . | nindent 4 }}
data:
  NETBOX_URL: {{ required "netbox.url is required" .Values.netbox.url | quote }}
